CREATE TABLE products (
                          id UUID PRIMARY KEY NOT NULL,
                          name VARCHAR(255),
                          description TEXT,
                          price DECIMAL(10, 2)
);
